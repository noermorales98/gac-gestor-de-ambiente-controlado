import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GAC - Gestor de Ambiente Controlado',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
